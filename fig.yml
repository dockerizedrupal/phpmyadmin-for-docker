phpmyadmin:
  image: viljaste/phpmyadmin:dev
  hostname: phpmyadmin
  ports:
    - "80:80"
    - "443:443"
  volumes_from:
    - phpmyadmindata
  environment:
    - SERVER_NAME=localhost
phpmyadmindata:
  image: viljaste/data:dev
  hostname: phpmyadmindata
  volumes:
    - /phpmyadmin
