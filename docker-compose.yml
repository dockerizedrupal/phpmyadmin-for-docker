phpmyadmin:
  image: dockerizedrupal/phpmyadmin:latest
  hostname: phpmyadmin
  ports:
    - "80:80"
    - "443:443"
  volumes_from:
    - phpmyadmindata
  environment:
    - SERVER_NAME=localhost
    - TIMEOUT=300
    - PROTOCOLS=https,http
    - USERNAME=root
    - PASSWORD=root
phpmyadmindata:
  image: dockerizedrupal/data:latest
  hostname: phpmyadmindata
  volumes:
    - /phpmyadmin
